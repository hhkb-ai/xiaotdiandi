# 首页优化提示词

> 适用于「晓点滴」(renjian-story-vue) 首页优化，可直接复制给 AI 使用。

---

## 项目背景

这是一个基于 Vite 8 + Vue 3 + Vue Router 4 的内容站点，线上域名 `apiuspro.cn`，源码在 `renjian-story-vue`，共 52 篇文章（stories.json），涵盖 AI API、教程、测评、场景推荐、错误解决、FAQ 等内容。响应式断点：桌面 >980px | 平板 ≤980px | 手机 ≤780px | 小手机 ≤520px。

## 优化目标

对 `src/views/HomeView.vue`（及关联组件）进行全面优化，同时遵守以下 LLM 编程准则：
- **先想再写**：明确假设，不确定就问
- **够用就行**：最小代码解决问题，不加多余抽象
- **改哪动哪**：只改必要的文件，不顺手"优化"无关代码
- **目标驱动**：每次改动都要能追溯到具体目标，做完有验证

## 优先级

### P0（必须修复）

1. **文章数量硬编码**：`HomeView.vue` 和 `tdk.json` 中硬编码"50 篇"，实际 `stories.json` 有 52 篇。改为从数据源动态读取，或统一同步。
2. **搜索无结果空状态**：搜索返回空时没有 UI 提示，用户不知道是没搜到还是出 bug。需加空状态视图（图标 + 提示文案 + 清除按钮引导）。

### P1（应该优化）

3. **桌面搜索栏无清除按钮**：`HomeView.vue` 中清除按钮仅 `isMobile` 时显示，桌面端用户无法一键清空搜索。让清除按钮在所有视口都可见。
4. **搜索建议硬编码**：搜索建议 `keywordList` 是写死的，不随内容更新。改为从文章数据动态生成热门关键词（如取出现频次最高的 tags）。
5. **首页搜索 vs 教程页搜索不一致**：首页 `useStories` 搜索全部 8 类内容，教程页只搜 apiList。统一为同一个搜索入口和数据源，或在教程页加说明。

### P2（锦上添花）

6. **搜索防抖**：输入时无防抖，每次按键都触发搜索。加 200-300ms debounce 提升体验。
7. **SEO 元数据**：确保首页 `<title>`、`<meta description>`、Open Graph 标签包含实际文章数量和核心关键词。
8. **首屏加载优化**：首页文章列表是否做了懒加载/虚拟滚动？如果文章数增长，需要考虑。
9. **深色模式适配**：首页各组件在深色模式下视觉是否一致，有无对比度不足的元素。
10. **移动端底部导航遮挡**：≤780px 时底部导航是否遮挡了页面内容，留足安全距离。

## 具体要求

- 只改 `renjian-story-vue` 源码，不要碰 `renjian-story-shop` 部署目录
- 改动后确保 `pnpm ts-check`、`pnpm lint`、`pnpm build` 全部通过
- 每个改动单独提交，commit message 说明改了什么、为什么改
- 如果某个优化点涉及多个文件，说明依赖关系，不要漏改
- 搜索算法的优化（如模糊匹配）如果成本太高，先只做空状态和清除按钮等 UI 层面的改进

## 验证清单

- [ ] 首页文章数量显示正确（52 篇，或动态读取）
- [ ] 搜索无结果时有空状态 UI
- [ ] 桌面端搜索栏可以清除输入
- [ ] 搜索建议关键词有合理来源
- [ ] 深色模式下首页各元素正常
- [ ] 移动端（390x844、375x667）布局无遮挡
- [ ] 构建无报错，lint 无新增 warning
