# 首页排版优化提示词

> 基于 HomeView.vue 及关联 CSS 文件的实际结构，可直接交给 AI 执行。

---

## 项目背景

Vue 3 + Vite 项目「晓点滴」，校园生活内容站。首页源码在 `src/views/HomeView.vue`，样式拆分在 `src/styles/` 下多个 CSS 文件。整体风格是暖色纸质感（背景 #f7f3ea），衬线字体为主、Inter 用于 UI 元素。

## 当前首页结构（从上到下）

1. Hero 双栏：左文案+搜索+CTA / 右精选轮播
2. Content-layout 双栏：280px 侧边栏（阅读信息）/ 右侧主体内容
   - Spotlight 首页主推（双栏：左文案+预览 / 右 checklist+受众）
   - Practical shelf 先看这些专题（4列卡片）
   - Latest 新更新（2列卡片）
   - Curated 首页精选文章（2列卡片）
   - Archive 全部目录（带排序的列表）
3. Control-row 分类筛选（3列 tab 按钮）
4. Keyword-cloud 常见话题
5. About 关于这个站

## 排版问题与优化方向

### 1. 页面节奏松散，缺乏呼吸感

当前每个 section 之间只有 1px border-bottom 分隔，section 内间距也不大。导致整页信息密度高但节奏平淡，读者无法区分"这一屏结束了，下一屏开始了"。

**具体改法：**
- 每个 `home-section` 之间增加 `padding-top: 48px`（当前 30px），底部也适当加大
- Spotlight 作为最重要的推荐位，上下各加一条较粗的装饰分隔或留白，让它视觉上"浮出来"
- Hero 区域底部到 content-layout 之间增加至少 `40px` 的过渡间距
- 全部目录（Archive）之前加更大的视觉间隔（如 `padding-top: 60px`），明确区分"精选区"和"浏览区"

### 2. 各 section 视觉权重趋同

Practical shelf（4列）、Latest（2列）、Curated（2列）三块内容的卡片样式几乎相同（feature-card），读者无法快速判断哪块更重要。

**具体改法：**
- Practical shelf 卡片尺寸加大（min-height 从 220px 提到 280px），标题字号从 20px 提到 22px，与其他 section 拉开层次
- Latest 区域改用更紧凑的样式：卡片 min-height 降到 160px，去掉 card-intent，只保留标题+摘要，让它读起来更像"快速浏览"
- Curated 区域给卡片加上左侧 3px 的强调色边框（类似 story-card:hover 的 inset box-shadow），与 Latest 视觉区分

### 3. Hero 区轮播功能弱、占位大

右侧 hero-feature 轮播一次只显示一张卡片，5 秒自动轮播，但左右箭头和分页点占了不少空间。左侧文案区和右侧轮播高度不对齐时，整体显得不平衡。

**具体改法：**
- 轮播改为同时展示 2 张卡片（grid 两列），去掉自动轮播和分页点，只保留左右箭头
- 或者：保持单张轮播但降低轮播区最小高度（当前 min-height 240px → 180px），让 hero 整体更紧凑
- 如果选择展示 2 张卡片，需要调整 `hero-feature` 的 grid 布局和卡片宽度

### 4. 侧边栏功能单薄

280px 的 sidebar 只放了"共 X 篇"、当前分类、3 个快捷链接，在桌面端浪费了宝贵的横向空间。

**具体改法：**
- 侧边栏增加"分类分布"小统计（每个分类几篇），用 mini bar chart 或简单数字列表展示
- 或者：侧边栏改为浮动 TOC，列出当前可见的 section 标题（Spotlight / 先看专题 / 新更新 / 精选 / 全部），滚动时高亮当前 section
- 如果不加功能，考虑将 sidebar 宽度缩小到 240px，让主体内容区更宽

### 5. 控制行位置反直觉

分类筛选（Control-row）放在所有精选内容**之后**、全部目录**之前**。用户要先看完 Spotlight + Practical + Latest + Curated 才能看到分类筛选。

**具体改法：**
- 将 Control-row 移到 content-layout 的顶部（story-area 的最前面），作为"我想看什么类型"的入口
- 或者在 Hero 区的搜索框旁边加一排分类快捷标签（pill 形式），让分类筛选在首屏就可见
- 两个位置可以同时存在：Hero 下方放精选分类 pill，当前位置的 Control-row 保留作为完整分类面板

### 6. 故事列表（Archive）缺少视觉分组

全部目录是纯列表，每行一个 story-card（编号 + 标题 + 摘要 + 按钮），52 篇全部展开时非常冗长。

**具体改法：**
- 按分类分组展示：每组一个分类标题（h4），下面跟该分类下的文章
- 每组之间增加分隔线或背景色差异
- 或者改为网格布局（3列卡片），比纯列表更节省空间

### 7. 关键词云（keyword-cloud）与分类筛选功能重复

关键词云（30个热门词）和 Control-row 的分类 tab 功能有重叠。两者都引导用户筛选内容，但放在不同位置。

**具体改法：**
- 合并两者：Control-row 的 tab 按钮内，每个分类下直接显示该分类的热门关键词
- 或者去掉 keyword-cloud section，将关键词集成到搜索框的 datalist 建议中
- 如果保留 keyword-cloud，移到搜索框下方作为搜索建议，而不是页面底部

## 字体与间距微调

- 标题 font-weight：当前 h1 用 500，h3 用 500，h4 用 600。建议 h1 保持 500，h3 改为 600 以增强层次
- body 正文（.hero-copy、.feature-card p）行高从 1.62/1.65 调到 1.7，提升阅读舒适度
- 所有卡片圆角统一为 8px（当前混用 6px 和 8px）
- card-tags 的 margin-bottom 从 16px 降到 10px，标签离标题更近

## 约束

- 只改 `src/` 下的源码，不碰部署目录
- 改完后 `pnpm ts-check && pnpm lint && pcss lint`（如有）通过
- 每个改动说明影响的文件和行号
- 不改变信息架构（section 顺序和内容源），只优化视觉呈现
- 保持深色模式兼容，所有新颜色用 CSS 变量
- 移动端响应式不能退化，改动需同步更新 responsive.css 中对应的断点规则
