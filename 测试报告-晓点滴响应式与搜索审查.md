# 晓点滴响应式与搜索审查报告

**测试日期**: 2026-06-02
**测试员**: Claude (源码审查 + 构建产物静态检查，浏览器实测因 Chrome 扩展未连接而受限)

---

## 一、结论

- **是否通过**: 部分通过 — 源码结构良好，响应式 CSS 设计合理，但存在 2 个 P0 问题和 3 个 P1 问题
- **最大风险**: `renjian-story-shop`（疑似线上部署目录）与源码完全不一致，线上用户看到的是旧版本
- **是否建议上线**: 不建议直接上线，需先修复 P0 问题并重新构建部署

---

## 二、环境

- **当前目录**: `D:\fenxxiang`
- **当前分支**: `main` (ahead of origin/main)
- **git status 摘要**: 21 个已修改文件（M），5 个未跟踪文件（??）。涉及 `renjian-story-vue`、`blog-vue`、`renjian-story-shop` 三个子项目
- **测试项目判断依据**:
  - `renjian-story-vue` 有 `package.json`（Vite + Vue 3），是唯一源码项目
  - `renjian-story-shop` 无 `package.json`，是静态部署/旧构建目录
  - `blog-vue` 是独立项目，本次不测试
- **启动/构建命令**: `npm run dev` / `npm run build` / `npm run preview`
- **本地访问地址**: `http://localhost:5173`（dev）/ `http://localhost:4173`（preview）
- **构建限制**: VM 环境 fuse 文件系统无法解压 native Node 模块（`@rolldown/binding-linux-x64-gnu`），`npm run build` 在 VM 中失败，需在 Windows 上执行

---

## 三、PC 响应式结果（源码审查）

### 断点设计
- 桌面: > 980px（默认）
- 平板: ≤ 980px（`.content-layout` 单列，sidebar 变横向）
- 手机: ≤ 780px（全面重排）
- 小手机: ≤ 520px（进一步简化）

| 视口 | 预判 | 依据 |
|------|------|------|
| 1920x1080 | 通过 | `width: min(1120px, calc(100% - 40px))` 限制内容宽度，hero 两列 grid，无溢出风险 |
| 1440x900 | 通过 | 同上，内容区最大 1120px，两侧留白 |
| 1366x768 | 通过 | 同上，仍大于 980px 断点，桌面布局完整 |

**注意**: 以上为源码审查预判，未经浏览器实际截图验证。

---

## 四、移动端响应式结果（源码审查）

| 视口 | 预判 | 依据 |
|------|------|------|
| 1024x768 | 通过 | 大于 980px 断点，桌面布局 |
| 768x1024 | 通过 | 触发 ≤780px 断点，sidebar 隐藏，mobile TOC 显示，category bar 横向滚动 |
| 430x932 | 通过 | ≤780px + ≤520px 双断点生效，grid 全单列，按钮 min-height: 44px |
| 414x896 | 通过 | 同上 |
| 390x844 | 通过 | 同上 |
| 375x667 | 通过 | 同上，hero h1 用 `clamp(26px, 7vw, 36px)` 自适应 |
| 360x740 | 通过 | 同上，body `min-width: 320px` 不会低于此值 |

**响应式亮点**:
- `safe-area-inset-left/right` 适配刘海屏
- category bar 移动端用 `overflow-x: auto` 横向滚动，不溢出
- 移动端 category panel 用 bottom sheet + overlay，不超出屏幕
- 所有交互元素 `min-height: 44px`，符合 Apple HIG 触摸目标
- `env(safe-area-inset-bottom)` 适配底部安全区

**潜在问题**:
- `body { min-width: 320px }` — 在 320px 宽设备上可能刚好，但无横向滚动保护（未见 `overflow-x: hidden`）

---

## 五、搜索栏结果（源码审查 + 逻辑分析）

### 基础搜索词测试（预判）

| 搜索词 | 预期结果 | 预判 | 说明 |
|--------|----------|------|------|
| 四六级 | 包含"四六级"的文章 | 通过 | searchKeywords 中应包含"四六级" |
| 宿舍 | 包含"宿舍"的文章 | 通过 | 校园生活类文章大概率包含 |
| 求职 | 包含"求职"的文章 | 通过 | 求职复盘类文章 |
| 简历 | 包含"简历"的文章 | 通过 | 求职类文章 |
| 毕业 | 包含"毕业"的文章 | 通过 | 毕业与回忆类 |
| 反诈 | 包含"反诈"的文章 | 通过 | 安全与情绪类 |
| 学生优惠 | 包含"学生优惠"的文章 | 通过 | 实用知识类 |
| 论文 | 包含"论文"的文章 | 通过 | 学习成长类 |
| 面试 | 包含"面试"的文章 | 通过 | 求职复盘类 |
| 选课 | 包含"选课"的文章 | 通过 | 学习成长类 |

### 模糊搜索/容错测试（预判）

| 搜索词 | 预期 | 预判 | 说明 |
|--------|------|------|------|
| 四级 | 部分匹配含"四级"的文章 | 通过 | `includes("四级")` 能匹配"四六级" |
| 六级 | 部分匹配含"六级"的文章 | 通过 | 同上 |
| 求值 | 无结果（错别字） | **失败** | `includes("求值")` 不会匹配"求职" |
| 简厉 | 无结果（错别字） | **失败** | `includes("简厉")` 不会匹配"简历" |
| 毕页 | 无结果（错别字） | **失败** | `includes("毕页")` 不会匹配"毕业" |
| 宿社 | 无结果（错别字） | **失败** | `includes("宿社")` 不会匹配"宿舍" |
| fanza | 无结果（拼音） | **失败** | 不支持拼音搜索 |
| xueshengyouhui | 无结果（拼音） | **失败** | 不支持拼音搜索 |
| " 求职 " | 应匹配（前后空格） | 通过 | `toLowerCase()` 后 `includes` 能匹配 |
| "毕业 档案" | 无结果（多词 AND） | **失败** | `includes("毕业 档案")` 要求连续匹配 |
| "求职 简历" | 无结果（多词 AND） | **失败** | 同上 |

### 搜索行为检查（预判）

| 检查项 | 预判 | 说明 |
|--------|------|------|
| 输入后实时显示结果 | 通过 | `@input="onSearch"` 触发 `applyKeyword` |
| 清空输入后恢复默认列表 | 通过 | `keyword.value = ''` 时 `matchStory` 返回 true |
| 搜索无结果时空状态 | **失败** | 未见空状态 UI，列表直接为空 |
| 搜索结果相关性 | 通过 | 搜索 title + summary + category + searchKeywords + keywords |
| 移动端/PC 搜索逻辑一致 | 通过 | 同一个 `useStories` composable |
| 中文输入法不提前触发 | **可能失败** | `@input` 在中文输入法组合时可能提前触发 |
| 回车/Tab/Esc 行为 | 不适用 | 无特殊处理，搜索是实时的 |
| 搜索结果点击后返回状态 | 通过 | 返回后 keyword 状态保留 |

---

## 六、模糊搜索审查

### 当前搜索算法能力

| 能力 | 支持 | 说明 |
|------|------|------|
| 精确匹配 | 是 | `fields.includes(q)` |
| 部分匹配 | 是 | 子串包含即匹配 |
| 错别字容错 | **否** | 无编辑距离/模糊匹配 |
| 拼音搜索 | **否** | 无拼音转换 |
| 多词 AND 搜索 | **否** | 要求连续子串匹配 |
| 权重排序 | **否** | 搜索结果无权重，按原始顺序 |
| 移动端/PC 一致 | 是 | 同一代码路径 |

### 关键代码分析

```javascript
// useStories.js 第 100-109 行
function matchStory(story, query) {
  const q = query.toLowerCase()
  const fields = [
    story.title,
    story.summary,
    story.category,
    ...(story.searchKeywords || []),
    ...(story.keywords || [])
  ].join(' ').toLowerCase()
  return fields.includes(q)
}
```

**问题**: 所有字段用空格拼接后做 `includes`，意味着：
1. "毕业 档案" 会被当作一个整体字符串匹配，而不是两个独立词
2. "简厉"（错别字）不会匹配 "简历"
3. 无搜索结果排序，匹配的文章按原始 ID 顺序返回

### 搜索数据源完整性
- 52 篇文章全部有 `searchKeywords`、`summary`、`content`
- 分类分布: 实用知识(19) > 学习成长(8) > 网络梗知识(6) > 求职复盘(5) > 校园生活(4) = 安全与情绪(4) = 毕业与回忆(4) > 活动与关系(2)
- 搜索覆盖: title + summary + category + searchKeywords + keywords — 无遗漏

---

## 七、发现的问题

### [P0] 阻塞上线

#### P0-1: renjian-story-shop 与源码完全不一致

- **问题描述**: `renjian-story-shop`（疑似线上部署目录）是完全不同的构建产物：
  - shop 声称"116篇"，实际 50 个 story 目录；源码 52 篇
  - shop 使用 Google Fonts（Noto Sans SC），源码使用系统字体
  - shop 缺少 `<!doctype html>` 声明
  - shop 的资源哈希（`vue-u3SVe0RQ.js`）与 dist（`index-CEWbwkXW.js`）不同
  - shop 的 meta description 与源码不同
- **复现步骤**: `diff renjian-story-vue/dist/index.html renjian-story-shop/index.html`
- **影响视口**: 所有视口
- **相关文件**: `renjian-story-shop/index.html`, `renjian-story-shop/story/`
- **建议修复方向**: 确认线上实际部署的是哪个目录。如果是 shop，需要用源码重新构建并替换整个 shop 目录。如果 shop 是旧版本，应删除或归档。
- **需要我确认**: 是 — 请确认线上 unietalk.cn 实际部署的是 shop 还是 dist

#### P0-2: HomeView 和 tdk.json 硬编码"50 篇"

- **问题描述**: `HomeView.vue` 第 5 行写死 `50 篇真实校园记录`，`tdk.json` 的 description 也写死 `50 篇`。但 `stories.json` 已有 52 篇文章。
- **复现步骤**: 查看 `HomeView.vue` 第 5 行；查看 `tdk.json` 第 4 行
- **影响视口**: 所有视口
- **相关文件**: `src/views/HomeView.vue` 第 5 行, `src/data/tdk.json` 第 4、7 行
- **建议修复方向**: HomeView 中改为 `{{ stories.length }} 篇真实校园记录`；tdk.json 中 description 需要动态生成或更新为正确数字
- **需要我确认**: 否 — 明确的数字错误

---

### [P1] 明显影响用户

#### P1-1: 搜索不支持模糊匹配、错别字、拼音

- **问题描述**: `matchStory()` 使用 `String.includes()` 做子串匹配，不支持：
  - 错别字（"简厉" 不匹配 "简历"）
  - 拼音（"xueshengyouhui" 不匹配 "学生优惠"）
  - 多词 AND（"毕业 档案" 不匹配 "毕业...档案"）
- **复现步骤**: 在搜索框输入"简厉"、"xueshengyouhui"、"毕业 档案"
- **影响视口**: 所有视口
- **相关文件**: `src/composables/useStories.js` 第 100-109 行
- **建议修复方向**: 短期可拆分多词为 OR 匹配；中期可引入简单编辑距离或拼音库
- **需要我确认**: 是 — 请确认是否需要实现模糊搜索

#### P1-2: 搜索无结果时无空状态提示

- **问题描述**: 搜索无匹配文章时，`displayStories` 为空数组，页面直接显示空白列表区域，无"未找到相关内容"提示
- **复现步骤**: 搜索一个不存在的词如"量子力学"
- **影响视口**: 所有视口
- **相关文件**: `src/views/HomeView.vue` 第 223-242 行（`.story-list` 区域）
- **建议修复方向**: 在 `.story-list` 前加 `v-if="displayStories.length === 0 && keyword"` 显示空状态
- **需要我确认**: 否

#### P1-3: CategoryView 与全局 category 状态潜在冲突

- **问题描述**: `CategoryView` 读取 `route.params.category` 但同时依赖 `useStories()` 的全局 `category` 状态。如果用户从首页选了"学习知识"大类，然后通过 URL 直接访问 `/category/校园生活`，全局 category 可能仍是"学习知识"，导致 `filteredStories` 过滤逻辑异常。
- **复现步骤**: 1) 首页点击"学习知识"大类 → 2) 地址栏手动输入 `/category/校园生活`
- **影响视口**: 所有视口
- **相关文件**: `src/views/CategoryView.vue` 第 96-103 行
- **建议修复方向**: CategoryView 应直接从 `stories` 全量数据中按 `story.category === route.params.category` 过滤，而非依赖 `filteredStories`
- **需要我确认**: 是 — 需确认实际表现

---

### [P2] 普通体验问题

#### P2-1: 搜索建议列表是硬编码的

- **问题描述**: `HomeView.vue` 中 `keywordList`（第 316-322 行）是固定数组，不从 stories.json 动态生成。新增文章的关键词不会自动出现在搜索建议中。
- **相关文件**: `src/views/HomeView.vue` 第 316-322 行
- **建议修复方向**: 从 `stories` 中提取去重的 `searchKeywords` 生成建议列表

#### P2-2: 搜索无防抖

- **问题描述**: `@input="onSearch"` 在每次输入事件时触发，中文输入法组合输入时可能频繁触发搜索。
- **相关文件**: `src/views/HomeView.vue` 第 19 行, `src/composables/useStories.js` 第 116-118 行
- **建议修复方向**: 添加 150-300ms debounce

#### P2-3: SPA 模式下 404 无自定义页面

- **问题描述**: 路由 catch-all `/:pathMatch(.*)*` 直接 redirect 到 `/`，不显示 404 页面。`dist/404.html` 存在但仅用于静态托管（Cloudflare Pages 等），SPA 内部导航不会触发。
- **相关文件**: `src/router/index.js` 第 26 行
- **建议修复方向**: 改 redirect 为 component 指向一个 404 页面组件

---

### [P3] 建议优化

#### P3-1: tdk.json 中 category 配置不完整

- **问题描述**: `tdk.json` 只有"校园日常"、"学习知识"、"安全情绪"三个大类的 SEO 配置，缺少 8 个具体子分类（校园生活、学习成长、实用知识等）的配置。访问 `/category/校园生活` 时会 fallback 到 home 配置。
- **相关文件**: `src/data/tdk.json`

#### P3-2: HomeView 搜索框 placeholder 与实际搜索能力不匹配

- **问题描述**: placeholder 写"例如：四六级、学生优惠、折扣、宿舍、考试周、课堂笔记"，但搜索实际不支持"折扣"（可能无此关键词的文章）。建议确保 placeholder 中的示例词都能搜到结果。

---

## 八、项目记忆写入内容

### 项目基本信息

- **实际源项目**: `D:\fenxxiang\renjian-story-vue`（Vite 8 + Vue 3 + Vue Router 4）
- **部署/静态目录**: `D:\fenxxiang\renjian-story-shop`（⚠️ 与源码不一致，需重新构建替换）
- **构建产物目录**: `D:\fenxxiang\renjian-story-vue\dist`（postbuild 脚本自动生成子页面）
- **启动命令**: `npm run dev`（dev）/ `npm run build` + `npm run preview`（生产预览）
- **线上域名**: `apiuspro.cn`（canonical）/ `unietalk.cn`（用户提及，需确认关系）

### 搜索相关关键文件

- `src/composables/useStories.js` — 搜索核心逻辑（`matchStory` 函数，第 100-109 行）
- `src/views/HomeView.vue` — 搜索框 UI + 搜索建议（第 10-25 行, 第 316-327 行）
- `src/data/stories.json` — 搜索数据源（52 篇，每篇有 searchKeywords）
- 搜索算法: 简单 `String.includes()` 子串匹配，无模糊/拼音/多词支持

### 响应式相关关键文件

- `src/styles/responsive.css` — 主响应式断点（980px / 780px / 520px）
- `src/styles/category-bar.css` — 分类栏移动端 bottom sheet
- `src/styles/hero.css` — 首页 hero 区域响应式
- `src/styles/detail.css` — 文章详情页布局
- `src/styles/variables.css` — CSS 变量 + 深色模式
- `src/views/CategoryView.vue` — 分类页自含响应式 CSS（768px 断点）

### 已验证通过的项

- 源码 52 篇文章全部有 searchKeywords、summary、content — 数据完整
- 响应式 CSS 断点设计合理，触摸目标 ≥44px
- 深色/浅色模式变量完整
- SEO: useSeo composable 动态更新 TDK + JSON-LD
- 无障碍: skip-link、aria-label、role="dialog"、aria-expanded

### 已发现但未修复的问题

1. [P0] renjian-story-shop 与源码完全不一致
2. [P0] HomeView/tdk.json 硬编码"50 篇"，实际 52 篇
3. [P1] 搜索仅支持子串匹配，无模糊/拼音/多词
4. [P1] 搜索无结果无空状态
5. [P1] CategoryView 与全局 category 状态潜在冲突
6. [P2] 搜索建议硬编码、无防抖、SPA 无 404 页

### 下次测试优先复查

1. **P0-1**: 确认线上实际部署的是 shop 还是 dist，统一部署源
2. **P0-2**: 修复"50 篇"硬编码
3. **P1-1**: 搜索算法升级（多词 OR、编辑距离）
4. **P1-2**: 搜索空状态 UI
5. **浏览器实测**: 本次因 Chrome 扩展未连接无法截图，需补测所有视口的实际渲染效果
