# 乔布斯视角：晓点滴首页诊断

## 乔布斯会说什么

> "People think focus means saying yes to the thing you've got to focus on. But that's not what it means at all. It means saying no to the hundred other good ideas."

当前首页的问题不是"不够好"，而是**想说的太多**。每个区块都在喊"看我看我"，结果读者进站后不知道该先做什么。

---

## 七大问题

### 1. Hero 是一场委员会会议

标题、副标题、搜索框、精选轮播——四个元素同时出现在首屏。乔布斯会问：**"用户进站第一秒，你想让他看到什么？"** 答案应该只有一个。当前的设计像一面贴满便利贴的墙，没有焦点。

**问题代码：** Hero 用了 `grid-template-columns: 1fr 1fr`，左边放标题+搜索，右边放轮播卡片。两个区块等权重，视线在左右之间跳跃，没有主次。

### 2. 轮播是噪音

自动播放的轮播（`setInterval 5000ms`）是乔布斯最反感的东西之一。它在用户没有主动选择的情况下改变内容，制造认知负担。Apple.com 从来没有自动轮播——要么是静态大图，要么是用户手动切换。

**更深层的问题：** 轮播里的内容和下方精选内容重复。同一篇文章在轮播里出现一次，在精选里又出现一次，在 archive 里再出现一次。同一个信息被展示了三次。

### 3. 区块太多，层级太乱

当前首页至少有 7 个独立区块：
- Hero（标题+搜索+轮播）
- 分类标签栏
- Spotlight（编辑推荐）
- Practical Shelf（实用专题）
- Latest（最新更新）
- Quality（精选文章）
- Archive（全部目录 + 排序）
- Keywords（话题云）
- About + Subscribe

7+ 个区块意味着用户要滚动 7 次才能看完首页。乔布斯会砍到 3 个：**一个 Hero、一个精选内容、一个收尾**。

### 4. 卡片信息过载

每张卡片包含：分类标签、日期、标题、摘要、practical takeaway、footer 分隔线。6 个元素挤在一张卡片里。乔布斯的设计原则是：**一张卡片只传递一个信息**。标题就是信息本身，不需要标签来告诉读者这是什么分类。

### 5. Sidebar 在浪费空间

`aside class="sidebar"` 里放了"校园故事"、"当前范围"、"快捷入口"三个区块。这些信息要么可以从其他地方推断（当前范围），要么完全不需要（快捷入口可以放在导航里）。在桌面端，sidebar 占了三分之一的宽度，却只传递了零信息量。

### 6. 视觉噪音太多

- 每张卡片都有 `border: 1px solid var(--line)`
- hover 时有 `box-shadow` + `translateY(-2px)` + `border-color: var(--ink)` 三重效果
- 分类标签用 `background: var(--ink)` 填充
- 每个区块都有 eyebrow 标签（"Featured"、"Recommended"、"Latest"）
- 轮播有圆点指示器 + 左右箭头 + "点击卡片阅读全文" 提示

这些装饰性元素加在一起，让页面看起来像一个"功能演示"，而不是一个"阅读入口"。

### 7. 搜索被藏在 Hero 里

搜索框放在 Hero 的副标题下方，作为一个普通的表单元素存在。但对于一个内容网站来说，搜索是**核心交互**——它应该被突出，而不是被淹没在 Hero 的众多元素中。

---

## 乔布斯会怎么做

**一个原则：减法。**

1. Hero 只留一句话 + 一个搜索框。去掉轮播，去掉副标题。
2. 精选内容只展示一篇文章，大字号标题 + 一行摘要。
3. 文章列表用单列，每篇只有标题 + 一句话。
4. 去掉 sidebar，去掉 eyebrow 标签，去掉卡片边框。
5. 分类用纯文字链接，不用 pill 按钮。
6. 颜色只用两个：墨色和灰色。去掉所有装饰色。
7. 搜索框无边框，只有底线。

*详见预览文件 `preview-jobs-ive-redesign.html`*
